# Penrose 2D ↔ PSC bridge

See the PSC-side bridge document in `larsbx/pisot-substitution-conjecture-research` for the status firewall.

## Short map

| Penrose concept | PSC analogue |
| --- | --- |
| residual boundary/interface patch | labelled first-return state |
| collar propagation | bounded local return/ancestry context |
| separator hit | prefix/boundary hit |
| formal interface vs legal patch | formal obstruction vs realized overlap |
| supertile hierarchy | substitution ancestry |
| symmetry quotient | finite-state normalization |

The strongest current transfer targets are P2/G1b-2 and P4/realization. P1b is only an analogy until the full non-unit representation-space issue is addressed.
