# Next steps

**Status:** execution queue.

## Priority order

1. **#1 — T1-A candidate class and literature audit.** This determines the theorem's legal hypothesis envelope and blocks theorem promotion elsewhere.
2. **#3 — T0 calibration corpus.** Build fixtures in parallel once candidate families are identified; do not wait for T2.
3. **#2 — T1-B exact overlap representation.** Implement only after `C_0` fixes the geometry and arithmetic requirements.
4. **#4 — T2 automatic coincidence.** Begin bad-SCC theorem work after T1 completeness is precise enough to know what a counterexample state means.
5. **#5 — kernel extraction gate.** Extract only interfaces stabilized by T0/T1 work.

## First research milestone

Call the first milestone **M1: exact class and overlap boundary**.

M1 is complete when:

- `C_0` is selected;
- the relevant literature theorem statements are pinned and hypothesis-mapped;
- the exact overlap state is specified;
- at least one positive and one negative fixture replay;
- every incomplete computation fails closed;
- T2 is stated as one explicit residual theorem, not a bundle of vague goals.

## Parallel work that is safe now

- literature source audit;
- exact fixture format;
- extraction inventory from PSC overlap code;
- comparison of scalar-displacement assumptions with vector-displacement needs;
- catalog of tile-intersection adapters.

## Work to defer

- broad class claims before #1;
- reusable-kernel extraction before interfaces stabilize;
- large censuses before the completeness theorem is specified;
- any theorem statement that uses Penrose geometry as a class hypothesis.
